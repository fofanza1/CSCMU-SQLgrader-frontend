import { AssignmentFilterPipe } from './assignment-filter.pipe';

describe('AssignmentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
