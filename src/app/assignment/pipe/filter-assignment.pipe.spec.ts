import { FilterAssignmentPipe } from './filter-assignment.pipe';

describe('FilterAssignmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssignmentPipe();
    expect(pipe).toBeTruthy();
  });
});
